// arithmetic operators = operands (values, variables, etc.)
//                        operator (+ - * /)
//                        ex. 11 = x + 5;

let students = 30;

// students = students + 1;
// students = students - 1;
// students = students * 2;
// students = students / 2;

// power of n
students = students ** 2;

// modular
students = students %2;

// shorcut(all possible)
students += 2;
students -= 2;
students *= 2;
students /= 2;
students **= 2;
students %= 2;

// 30:22
console.log(students);